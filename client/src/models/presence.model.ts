export type FillPresenceIntrf = {
    status: string;
}

export type MakePresenceIntrf = {
    classname: string;
    deadline: string;
    start_time: string;
}

export type PresenceIntrf = {
    created_at: string;
    classname: string;
    deadline: string;
    master_id: string;
    status: string;
    start_time: string;
    student_id: string;
    student_name: string;
}